{
  "name": "e2e-test-app-broken",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"console.error('intentional build failure for e2e failure-path scenario'); process.exit(1)\"",
    "start": "node -e \"console.log('never runs')\""
  }
}
